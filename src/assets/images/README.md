# مجلد صور المصدر (Src Assets Images)

يمكنك وضع الصور هنا واستيرادها (Import) مباشرة داخل مكونات React:

### مثال الاستخدام:
```jsx
import perfumeImg from '../assets/images/perfume1.png';

function Product() {
  return <img src={perfumeImg} alt="عطر" />;
}
```
